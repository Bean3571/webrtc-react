{
  "name": "webrtc-react",
  "version": "1.0.0",
  "main": "dist/index.js",
  "author": "Anna",
  "license": "MIT",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix || true",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn build",
    "start": "nodemon .",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "peer": "^0.6.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.18",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "tslint": "^6.1.3",
    "typescript": "^4.5.5"
  }
}
